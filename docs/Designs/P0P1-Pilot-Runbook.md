# 采购生成器 P0 —— 第一张真实 Pilot 操作手册

> 状态:**Production Gate = WAITING FOR 1 REAL PILOT ORDER**。
> P0 代码已 PASS(工程实现),但真实单没跑过之前不算完成。
> 前置:[Architecture Cut](./P0P1-Procurement-Architecture-Cut.md) · [Truth Map](./P0P1-Procurement-Truth-Map.md)

---

## 0. 这一张要证明什么

不是"代码能跑",而是这句产品承诺:

> **跟单做完 BOM 后,不再需要知道采购系统下一步在哪个按钮;系统自己把正确的工作送到采购面前。**

如果这一点在真实员工手里成立,P0 才真正完成。

---

## 1. 先拿 baseline(在开 Pilot 之前)

**一旦新流程开始试用,旧流程就再也测不到了。**

采集表已存在:[P0P1-Baseline-采集表.md](./P0P1-Baseline-采集表.md) —— 它已经覆盖需要的五项:

| CEO 要的字段 | 采集表对应 |
|---|---|
| 跟单做原辅料 Excel:__分钟 | 第一部分 ③ |
| 采购整理采购单:__分钟 | 第二部分 ③ |
| 手算:__次 | 第一部分 ④ + 第二部分 ④ |
| 重复输入:__字段 | 第一部分 ⑤ + 第二部分 ⑤ |
| 最终手工文件:__份 | 第一部分 ⑥ + 第二部分 ⑥ |

**一张正常的非 Pilot 单就够。** 挑单规则写在表里:不要挑之后要进试点的那几张 EHL 单。

---

## 2. 选第一张 Pilot 单

| 条件 | 说明 |
|---|---|
| 新的 EHL 常规订单 | **不要**拿 1022977 / 1022982 这类被反复修过的样本 |
| BOM 刚开始或尚未提交 | 提交那一跳正是要验的 |
| 正常交期 | 不要赶单 —— 出问题时人没时间配合排查 |
| 跟单 = 菁菁 | 优先 |
| 采购 = 真正负责这张单的人 | 不要找人代跑 |
| 没有已下采购单的历史动作 | 有历史 PO 会触发「补采购」分支,污染验收 |

---

## 3. 开闸(**只开一张**)

Vercel 环境变量:

```
PROCUREMENT_GENERATOR_PILOT = <这一张的订单号>
PROCUREMENT_GENERATOR_MAX   = 1
```

不是一上来 3–5 张。第一张跑通没问题,再扩大。

**回滚**:`PROCUREMENT_GENERATOR_PILOT` 改回 `off`,新链路即刻全灭,老路径一行没动。
不需要回滚代码、不需要改库。

---

## 4. 跑一遍(真人操作,不要代跑)

1. 跟单在订单里录 BOM —— **故意留一个物料不填 consumption_basis**;
2. 点「提交采购」→ 期望看到 🟠 `NEEDS_BOM_CONFIRMATION`,**并点名到那个物料**;
3. 跟单补上口径,再次提交 → 期望看到 🟢 `系统已自动生成待采购需求(N 项)`;
4. 跟单**到此为止,不再点任何按钮**;
5. 采购登录,打开采购中心 → 期望第一眼就看到「待采购 N 项」。

第 4 步是整个 P0 的核心:**跟单不该知道「归并」这个词。**

---

## 5. 验收 8 项 —— 谁来判

```bash
npx tsx scripts/verify-procurement-p0-pilot.mts <订单号>
```

脚本**只读**,不写任何东西。它严格区分三类:

| # | 验收项 | 判定方 |
|---|---|---|
| 1 | 非 Pilot 单完全无变化 | 🧑 人工(找一张名单外的单,确认台账仍可新增) |
| 2 | basis 缺失停在 NEEDS_BOM_CONFIRMATION | 🧑 人工(看页面)+ 🤖 断言②(库里口径缺口数) |
| 3 | 不需要再找「归并」按钮 | 🧑 人工 + 🤖 断言⑥(MRP 与首个采购项相差 ≤120s = 同一次提交内自动完成) |
| 4 | 自动产生正确的 procurement_items | 🤖 断言④ |
| 5 | 数量与 BOM / consumption_basis 对账 | 🧑 **人工看对账表**(脚本打表不给结论,理由见下) |
| 6 | 采购页面出现「待采购 N 项」 | 🧑 人工 + 🤖 断言④ 的 N |
| 7 | 无孤儿 execution line | 🤖 断言⑦ |
| 8 | `lint:data-access` 保持 2324 不涨 | 🤖 `npm run lint:data-access` |

**为什么第 5 项不给机器结论**:`total_required_qty` 面料走「件数 × 大货单耗」、
客供/加工厂承担的行整行跳过 —— 它**不是** `Σ net_purchase_qty`。
在验收脚本里复刻一遍归并算法,就是造第二套真相,正是 ADR-006 要防的东西。
所以脚本只打出「物料 / 色 / 单位 / 总需求 / 建议采购 / 来源数」对账表,由跟单和采购看数字对不对。

脚本另外还断言了三条结构守恒(不重算数量):项数 ≤ 需求数、Σ来源数 ≤ 需求数(防重复计入)、
归并键唯一、无零来源项、无空数量项。

---

## 6. 一个现在还证明不了的不变量

> Pilot production `procurement_line_items` 一旦产生,100% 必须能反查 `procurement_item_id`。

**P0 阶段执行行本就为 0** —— 它们要等采购确认采购项之后才生成(自动确认 = 系统替人做商业承诺,违反 DP-4)。

所以现在断言⑦只能证明**入口被堵住了**(旧对账入口、legacy 台账桥、7 个 tracking writer 全部服务端硬拒绝),
不能证明生产数据最终满足这个不变量。**这一条要等 P1 采购确认跑通后复验。**

---

## 7. 完成标准

- 可断言项全部 PASS(脚本 exit 0);
- 人工三项确认成立;
- **跟单没有问过"接下来点哪里"**。

达成 → Production Gate PASS,再扩到 3–5 张。
任一断言 FAIL → `PROCUREMENT_GENERATOR_PILOT=off`,先停,不要扩大。

---

# 附录 A · Pilot 单选定与辅料分配增补(2026-08-16 CEO 拍板并入 P0)

P0 范围已扩:BOM 侧补上**辅料 SKU 分配能力**(`allocation_mode`)。
理由:Pilot 的核心承诺是「跟单录一次,采购不用再录」。辅料在 BOM 侧连色×码都表达不了,
Pilot 从第一步就不完整,只会得到「面料链通了、辅料仍回 Excel」的假结论。

## A.1 选定的 Pilot 单

**QM-20260806-013(PO 79132)** —— 只开这一张。

| 维度 | 实况 | 是否满足 CEO 条件 |
|---|---|---|
| 客户 / 用途 | EHL / `production` 常规大货 | ✅ 非打样 |
| 数量 | 960 件(件/套=1,非套装) | ✅ 体量适中,出错代价低 |
| 款 / 色 | 款 31548U;**2 色**(黑色 600 套 / 灰棕色 360 套) | ✅ ≥2 色 |
| 尺码 | 每色 4 码(S/M/L/XL)齐全 | ✅ 够咬 `by_style_color_size` |
| 面料 | BOM 已有 **2 行面料** | ✅ 含 fabric |
| 辅料 | BOM **0 行** —— 正是要 Pilot 补的 | ✅ BOM 尚未完全提交 |
| 采购动作 | 采购项 0 条,`procurement_order_placed` = pending | ✅ 无历史采购动作 |
| 交期 | 2026-09-25(约 6 周) | ✅ 正常交期,不赶 |
| 负责人 | **菁菁**(merchandiser, cathy@qimoclothing.com) | ✅ 本人 dogfood |
| 采购 | 在职 buyer:吴彦洺 / 王一品(指定 1 人) | ✅ 1 名真实 buyer |

备选(只在 013 因业务原因不可用时启用,仍只开一张):
- `QM-20260806-018`(PO 79133)—— 3 色 2160 件,覆盖更宽,体量更大;
- `QM-20260815-001`(PO 79154)—— 2 色 216 件,交期 10-05 最宽松,风险最低。

## A.2 这张单要咬到的四种物料

菁菁在 BomTab 录辅料时,**至少**覆盖:

| 物料 | 分配方式 | 口径 | 预期系统算出 |
|---|---|---|---|
| 面料(已有 2 行) | 整单 / 按款色 | 面料口径 | 走原路径,不分码 |
| 主标 | `whole_order` 整单 | `PER_SET` 1/套 | 960 |
| 洗标 | `whole_order` 整单 | `PER_SET` 2/套 | 1920 |
| **尺码牌** | **`by_style_color_size`** | `PER_SET` 1/套 | 黑 S100/M200/L200/XL100;灰棕 S60/M120/L120/XL60 |

尺码牌那 8 个数字**一个都不许她手输** —— 选完分配方式就该自己出现。这是本次增补的验收核心。

## A.3 两遍跑法(不要跳过第一遍)

**第一遍 · 故意留缺口**
留一个辅料的 `consumption_basis` 不确认 → 提交采购。
预期:`NEEDS_BOM_CONFIRMATION`,点名缺口物料,`nextActor = merchandiser`,
不生成可确认采购,**不猜**。同时那个料的分配预览应显示"未确认用量口径…系统不会替你猜"。

**第二遍 · 补齐**
补上口径 → 重新提交。
预期:系统**自动**推进 物料需求 → 采购项 → 待采购 N 项,
**不需要有人知道去点「归并」**;尺码牌的 8 格随之进入 `sku_breakdown`。

## A.4 新增人工验收(CEO 定,权重最高)

机器验收(见正文第 5 节 + `scripts/verify-trim-allocation-p0.mts`)只能证明**系统算得出来**,
不能证明**业务执行敢拿它工作**。所以增加一条,由菁菁本人回答:

> **「不给你原来那份 Excel,只看这个页面,你能不能确认这张辅料采购需求是对的?」**

- 答 **YES** → System-First 成立。这一张 Pilot 的真正目标达成:
  一个真实跟单**第一次没有做那张 Excel**。
- 答 **NO** → 记录**具体缺什么**(不是"感觉不好用"),据此改 UI。
  只有在她实际用过之后仍然说"特殊辅料还是 Excel 快很多",才考虑启动 Excel-First Bridge。

**不要现在同时开发第二入口。** 系统已经拥有那些数量,先验证 System-First 能不能让她根本不需要做那张 Excel。

## A.5 边界

- 不开发 P1;不开发 Excel Parser;不扩大 Pilot。
- 一张真实 Pilot 完成后 **STOP**。
