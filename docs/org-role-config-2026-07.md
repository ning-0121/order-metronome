# 用户角色配置清单（2026-07）—— 请方圆按此配置

> 操作入口：用 **admin 账号**登录 → 打开 **`/admin/users`（用户管理）** → 对每人点「**编辑角色**」→ 勾选下表指定角色 → 「**确认保存**」。
> 括号/加粗即界面上会看到的角色名。权威角色↔部门映射见 [`org-system-role-mapping.md`](./org-system-role-mapping.md)；系统**角色驱动、从不写死人名**，换人只改这里，零代码。

**前提**：同事需先各自**注册登录过系统**，`/admin/users` 里才有其账号。名单里没有的人，先让本人注册一次。每人只勾**一个**角色（除非本人身兼多职，可多勾）。

---

## 一、业务开发部（PO 前：客户开发／报价／PO 确认）

| 姓名 | 勾选角色 | 系统 value |
|------|----------|-----------|
| 陈洁 | **开发业务经理** | `sales_manager` |

## 二、业务执行部（PO 确认后：一路跟到出货）

| 姓名 | 勾选角色 | 系统 value |
|------|----------|-----------|
| 高洁 | **订单管理经理** | `order_manager` |
| 陈陈 | **业务执行** | `merchandiser` |
| 菁菁 | **业务执行** | `merchandiser` |
| 欧璐 | **业务执行** | `merchandiser` |
| 小英 | **业务执行** | `merchandiser` |
| Winnie | **业务执行** | `merchandiser` |
| 灵珑 | **业务执行** | `merchandiser` |

## 三、生产部

| 姓名 | 勾选角色 | 系统 value |
|------|----------|-----------|
| 秦增富 | **生产主管** | `production_manager` |
| 骆淑娟 | **生产跟单（含QC）** | `production` |

---

## 四、EHL 客户换负责人（**不在** /admin/users）

> 等菁菁上面配成「业务执行」后再做。由 **高洁或 admin** 操作：

1. 逐张打开 **EHL 的在途订单**
2. 订单详情找到「**理单跟单**」→ 点「**更换**」
3. 选 **菁菁** → 确认

---

## 配完的效果

- **高洁** 能给业务执行组（陈陈/菁菁/欧璐/小英/Winnie/灵珑）派单，改派走订单页「理单跟单 → 更换」。
- **秦增富** 只能派「生产跟单/QC」（骆淑娟这类），不再能派业务执行组。
- 订单/节点上的角色名显示正确：业务开发 / 业务执行 / 生产跟单。
- 新单的 PO 后业务执行节点默认挂在**建单人**名下，高洁按需改派给具体理单员。

## 注意

- **May 的角色不用动**（仍是业务执行/`merchandiser`）；本次只把 EHL 这个客户从她转给菁菁。
- 一人身兼多职可多勾（例：方圆 = 财务 + 行政督办）。
- 只改「角色」安全；不要改人的**邮箱**（登录凭据）。
- 管理员白名单（`admin`）：alex@qimoclothing.com、su@qimoclothing.com —— 需要新增 admin 找他们。

---
*配置口径依据组织架构 2026-07；系统实现见 `lib/domain/roles.ts`、`components/UserRoleManager.tsx`、`lib/utils/i18n.ts`。*
